{
  "name": "@gsagents/openhands",
  "version": "1.0.0",
  "scripts": {
    "dev": "python -m uvicorn src.main:app --reload --port 8000",
    "start": "python -m uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "install:deps": "uv pip install -r requirements.txt"
  }
}